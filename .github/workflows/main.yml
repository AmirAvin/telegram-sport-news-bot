name: Main Yamal

on:
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: football-bot
  cancel-in-progress: false

jobs:
  run-bot:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Bot Disabled
        run: |
          echo "========================================"
          echo "FOOTBALL NEWS BOT IS TEMPORARILY DISABLED"
          echo "NO NEWS WILL BE SENT"
          echo "========================================"
