name: Telegram Sports News Bot v2

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  bot-disabled:
    runs-on: ubuntu-latest

    steps:
      - name: News Bot Disabled
        run: |
          echo "========================================"
          echo "NEWS BOT IS TEMPORARILY DISABLED"
          echo "NO NEWS WILL BE SENT"
          echo "========================================"
